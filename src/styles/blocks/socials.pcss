.socials {
  display: flex;
  justify-content: center;
  align-items: center;
}

.socials__item {
  margin-right: 40px;

  &:last-child {
    margin-right: 0;
  }
}

.socials__icon {
  fill: rgba(#fff, 0.3);
  transition: 0.3s;
  width: 25px;
  height: 25px;

  &:hover {
    fill: #fff;
  }
}
